{
  "name": "shape-from-shading",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm run wasm && vite build",
    "wasm": "make -C cpp"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.3.5"
  }
}